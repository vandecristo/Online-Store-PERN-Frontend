import styles from './styles.module.scss';

const Favorites = () => {
    return (
        <div className={styles.favorites}>
            favorites
        </div>
    );
};

export default Favorites;
