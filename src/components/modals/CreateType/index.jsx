const CreateType = () => {

    return (
        <form>
            <title>Create Type:</title>
            <input type="text" placeholder='name'/>
        </form>
    );
};

export default CreateType;
